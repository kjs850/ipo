# Design: 부동산 청약 일정 기능

> Plan 문서: `docs/01-plan/features/real-estate-subscription.plan.md`

## 1. 시스템 아키텍처

### 1.1 전체 구조

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend                              │
│  ┌─────────────┐  ┌───────────────────┐  ┌───────────────┐  │
│  │ TabSelector │──│  IPOCalendar      │  │RealEstate     │  │
│  │             │  │  (기존)            │  │Calendar (신규) │  │
│  └─────────────┘  └───────────────────┘  └───────────────┘  │
│         │                  │                     │           │
│         └──────────────────┴─────────────────────┘           │
│                            │                                  │
│                      page.tsx (탭 상태 관리)                   │
└────────────────────────────┼─────────────────────────────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
              ┌─────▼─────┐    ┌──────▼──────┐
              │ /api/ipo  │    │/api/real-   │
              │ (기존)     │    │estate (신규) │
              └─────┬─────┘    └──────┬──────┘
                    │                 │
              ┌─────▼─────┐    ┌──────▼──────┐
              │ crawler.ts│    │realEstate   │
              │ (38.co.kr)│    │Crawler.ts   │
              └───────────┘    └──────┬──────┘
                                      │
                               ┌──────▼──────┐
                               │공공데이터포털│
                               │    API      │
                               └─────────────┘
```

### 1.2 데이터 흐름

```
1. 사용자가 "부동산" 탭 클릭
2. page.tsx에서 activeTab 상태 변경
3. RealEstateCalendar 컴포넌트 렌더링
4. useEffect에서 /api/real-estate 호출
5. API Route에서 캐시 확인 → 없으면 공공데이터포털 API 호출
6. realEstateCrawler.ts에서 데이터 변환
7. JSON 응답 반환
8. UI 렌더링 + 자동 스크롤
```

## 2. API 설계

### 2.1 공공데이터포털 API 연동

**Base URL**: `https://api.odcloud.kr/api/ApplyhomeInfoDetailSvc/v1`

**사용 엔드포인트**:
| 엔드포인트 | 설명 | 용도 |
|-----------|------|------|
| `/getAPTLttotPblancDetail` | APT 분양정보 상세 | 아파트 청약 일정 |
| `/getUrbtyOfctlLttotPblancDetail` | 오피스텔/도시형 상세 | 오피스텔 청약 일정 |

**요청 파라미터**:
```typescript
interface ApiRequest {
  page: number;           // 페이지 번호 (기본: 1)
  perPage: number;        // 페이지 크기 (기본: 100)
  returnType: 'JSON';     // 응답 형식
  serviceKey: string;     // API 인증키
  // 날짜 필터 (선택)
  'cond[RCRIT_PBLANC_DE::GTE]'?: string;  // 모집공고일 시작
  'cond[RCRIT_PBLANC_DE::LTE]'?: string;  // 모집공고일 종료
}
```

**응답 필드 매핑**:
| API 필드 | 내부 필드 | 설명 |
|----------|----------|------|
| HOUSE_MANAGE_NO | id | 주택관리번호 |
| PBLANC_NO | announcementNo | 공고번호 |
| HOUSE_NM | name | 주택명 |
| HSSPLY_ADRES | location | 공급위치 |
| SUBSCRPT_AREA_CODE_NM | region | 공급지역명 |
| RCRIT_PBLANC_DE | announcementDate | 모집공고일 |
| RCEPT_BGNDE | subscriptionStartDate | 청약접수시작일 |
| RCEPT_ENDDE | subscriptionEndDate | 청약접수종료일 |
| PRZWNER_PRESNATN_DE | winnerAnnouncementDate | 당첨자발표일 |
| CNTRCT_CNCLS_BGNDE | contractStartDate | 계약시작일 |
| CNTRCT_CNCLS_ENDDE | contractEndDate | 계약종료일 |
| TOT_SUPLY_HSHLDCO | totalSupply | 공급세대수 |
| HOUSE_SECD_NM | buildingType | 주택유형 |
| MVN_PREARNGE_YM | moveInDate | 입주예정월 |
| HMPG_ADRES | detailUrl | 홈페이지 주소 |

### 2.2 내부 API 엔드포인트

**`GET /api/real-estate`**

**Query Parameters**:
| 파라미터 | 타입 | 기본값 | 설명 |
|---------|------|--------|------|
| force | boolean | false | 캐시 무시 |
| type | string | 'apt' | 'apt' \| 'officetel' \| 'all' |

**Response**:
```typescript
interface ApiResponse {
  source: 'cache' | 'live';
  updatedAt: string;  // ISO 8601
  data: RealEstateSubscription[];
}
```

## 3. 데이터 모델

### 3.1 타입 정의

```typescript
// src/lib/realEstateCrawler.ts

export interface RealEstateSubscription {
  // 식별자
  id: string;                     // 주택관리번호
  announcementNo: string;         // 공고번호

  // 기본 정보
  name: string;                   // 주택명 (단지명)
  region: string;                 // 공급지역명 (서울, 경기 등)
  location: string;               // 공급위치 (상세 주소)
  buildingType: string;           // 주택유형 (APT, 오피스텔 등)

  // 일정 정보
  announcementDate: string;       // 모집공고일 (YYYY-MM-DD)
  subscriptionStartDate: string;  // 청약접수시작일 (YYYY-MM-DD)
  subscriptionEndDate: string;    // 청약접수종료일 (YYYY-MM-DD)
  winnerAnnouncementDate: string; // 당첨자발표일 (YYYY-MM-DD)
  contractStartDate?: string;     // 계약시작일
  contractEndDate?: string;       // 계약종료일
  moveInDate?: string;            // 입주예정월 (YYYY-MM)

  // 공급 정보
  totalSupply: number;            // 공급세대수

  // 상태 (계산 필드)
  status: '예정' | '진행' | '마감';

  // 링크
  detailUrl?: string;             // 상세 공고 URL
}
```

### 3.2 상태 계산 로직

```typescript
function calculateStatus(item: RawApiData): '예정' | '진행' | '마감' {
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const startDate = parseDate(item.RCEPT_BGNDE);
  const endDate = parseDate(item.RCEPT_ENDDE);

  if (!startDate || !endDate) return '예정';

  if (today < startDate) return '예정';
  if (today > endDate) return '마감';
  return '진행';
}
```

## 4. 컴포넌트 설계

### 4.1 TabSelector 컴포넌트

**파일**: `src/components/TabSelector.tsx`

```typescript
interface TabSelectorProps {
  activeTab: 'ipo' | 'real-estate';
  onTabChange: (tab: 'ipo' | 'real-estate') => void;
}
```

**디자인 스펙**:
- 2개 탭: "공모주" | "부동산"
- 활성 탭: 밝은 배경 + 하이라이트 색상
- 비활성 탭: 어두운 배경
- 전환 애니메이션: 0.2s ease
- 위치: 헤더 섹션 아래, 캘린더 위

**UI 스케치**:
```
┌─────────────────────────────────────┐
│  ┌──────────┐  ┌──────────────┐     │
│  │  공모주   │  │   부동산     │     │
│  │  (활성)   │  │   (비활성)   │     │
│  └──────────┘  └──────────────┘     │
└─────────────────────────────────────┘
```

### 4.2 RealEstateCalendar 컴포넌트

**파일**: `src/components/RealEstateCalendar.tsx`

**Props**: 없음 (내부에서 데이터 fetch)

**State**:
```typescript
const [data, setData] = useState<RealEstateSubscription[]>([]);
const [loading, setLoading] = useState(true);
const [highlightedId, setHighlightedId] = useState<string | null>(null);
```

**카드 레이아웃**:
```
┌────────────────────────────────────────────────┐
│ [진행] 🏢 민간분양                              │
│                                                │
│ 래미안 원베일리                                 │
│ 📍 서울특별시 서초구 반포동                      │
│                                                │
│ ─────────────────────────────────────────────  │
│                                                │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ │
│  │ 공급세대수  │ │ 청약기간   │ │ 당첨발표   │ │
│  │   1,234    │ │02.01~02.05│ │  02.15    │ │
│  └────────────┘ └────────────┘ └────────────┘ │
│                                                │
│ ┌────────────────────────────────────────────┐│
│ │ 🏠 입주예정: 2028-03                        ││
│ └────────────────────────────────────────────┘│
└────────────────────────────────────────────────┘
```

**색상 스펙**:
| 상태 | 배지 색상 | 배경 |
|------|----------|------|
| 예정 | emerald-600 | emerald-500 shadow |
| 진행 | indigo-600 | indigo-500 shadow + ring |
| 마감 | slate-700 | slate-600 |

### 4.3 page.tsx 수정

**변경 사항**:
1. `activeTab` state 추가
2. TabSelector 컴포넌트 삽입
3. 조건부 렌더링 (IPOCalendar / RealEstateCalendar)
4. 헤더 텍스트 동적 변경

```typescript
// 변경 전
export default function Home() {
  return (
    <>
      <Navbar />
      <div className="container">
        <section>헤더</section>
        <IPOCalendar />
      </div>
    </>
  );
}

// 변경 후
'use client';

export default function Home() {
  const [activeTab, setActiveTab] = useState<'ipo' | 'real-estate'>('ipo');

  return (
    <>
      <Navbar />
      <div className="container">
        <section>동적 헤더</section>
        <TabSelector activeTab={activeTab} onTabChange={setActiveTab} />
        {activeTab === 'ipo' ? <IPOCalendar /> : <RealEstateCalendar />}
      </div>
    </>
  );
}
```

## 5. 파일 구조 및 구현 순서

### 5.1 파일 목록

| 순서 | 파일 경로 | 작업 | 타입 |
|------|----------|------|------|
| 1 | `src/lib/realEstateCrawler.ts` | 신규 | API 클라이언트 |
| 2 | `src/app/api/real-estate/route.ts` | 신규 | API 엔드포인트 |
| 3 | `src/components/TabSelector.tsx` | 신규 | UI 컴포넌트 |
| 4 | `src/components/RealEstateCalendar.tsx` | 신규 | UI 컴포넌트 |
| 5 | `src/app/page.tsx` | 수정 | 탭 통합 |
| 6 | `.env.local` | 수정 | 환경변수 추가 |

### 5.2 구현 순서 (체크리스트)

**Phase 1: 백엔드**
- [ ] 1.1 `.env.local`에 `REAL_ESTATE_API_KEY` 추가
- [ ] 1.2 `realEstateCrawler.ts` 작성
  - [ ] RealEstateSubscription 인터페이스 정의
  - [ ] fetchRealEstateData() 함수 구현
  - [ ] 날짜 파싱 및 상태 계산 로직
  - [ ] 에러 핸들링
- [ ] 1.3 `/api/real-estate/route.ts` 작성
  - [ ] 캐시 로직 (1시간 TTL)
  - [ ] GET 핸들러
  - [ ] force 파라미터 처리

**Phase 2: 프론트엔드**
- [ ] 2.1 `TabSelector.tsx` 작성
  - [ ] Props 타입 정의
  - [ ] 탭 UI 구현
  - [ ] 활성 상태 스타일링
- [ ] 2.2 `RealEstateCalendar.tsx` 작성
  - [ ] 데이터 fetch 로직
  - [ ] 카드 UI 구현
  - [ ] 자동 스크롤 기능
  - [ ] 로딩 상태
- [ ] 2.3 `page.tsx` 수정
  - [ ] 'use client' 지시문 추가
  - [ ] activeTab state 추가
  - [ ] TabSelector 연동
  - [ ] 조건부 렌더링

**Phase 3: 테스트 및 배포**
- [ ] 3.1 로컬 테스트
- [ ] 3.2 Vercel 환경변수 설정
- [ ] 3.3 배포 및 검증

## 6. 환경 설정

### 6.1 환경 변수

```env
# .env.local
REAL_ESTATE_API_KEY=<공공데이터포털에서_발급받은_API_KEY>
```

### 6.2 Vercel 설정 (vercel.json)

```json
{
  "crons": [
    {
      "path": "/api/ipo?force=true",
      "schedule": "0 7 * * *"
    },
    {
      "path": "/api/real-estate?force=true",
      "schedule": "0 8 * * *"
    }
  ]
}
```

## 7. 에러 처리

### 7.1 API 에러 처리

| 에러 코드 | 원인 | 대응 |
|----------|------|------|
| 401 | API 키 오류 | 환경변수 확인 로그 |
| 429 | Rate Limit | 재시도 + 캐시 연장 |
| 500 | 서버 오류 | 빈 배열 반환 + 에러 로그 |
| TIMEOUT | 5초 초과 | 빈 배열 반환 + 경고 |

### 7.2 UI 에러 상태

```typescript
// RealEstateCalendar.tsx
{!loading && data.length === 0 && (
  <div className="text-center py-20 text-gray-400">
    예정된 청약 일정이 없습니다.
  </div>
)}
```

## 8. 향후 확장 고려사항

| 기능 | 우선순위 | 구현 시점 |
|------|----------|----------|
| 지역별 필터 | P2 | v2.0 |
| 공급유형 필터 | P2 | v2.0 |
| 알림 기능 | P3 | v3.0 |
| 관심 청약 북마크 | P3 | v3.0 |

---

**문서 버전**: v1.0
**작성일**: 2026-02-04
**상태**: Ready for Implementation
**Plan 참조**: `real-estate-subscription.plan.md`
